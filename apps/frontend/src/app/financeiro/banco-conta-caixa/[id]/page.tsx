import { PageContainer } from "@/components/ui/page-container";`nimport { BancoContaCaixaForm } from "../_components/form";`nimport { getBancoContaCaixaById } from "@/actions/banco-conta-caixa";`n`nexport default async function BancoContaCaixaEditPage({ params }: { params: Promise<{ id: string }> }) {`n  const resolvedParams = await params;`n  const id = parseInt(resolvedParams.id);`n  const data = await getBancoContaCaixaById(id);`n`n  return (`n    <PageContainer>`n      <div className="max-w-2xl mx-auto">`n        <BancoContaCaixaForm initialData={data} />`n      </div>`n    </PageContainer>`n  );`n}